# C++ Tutorials

A collection of C++ programs covering fundamental concepts, built as a personal learning reference.

## Topics Covered

| File | Topic |
|------|-------|
| `cpp_tut.cpp` | Basics — strings, user input (`cin`/`getline`), `cmath` |
| `if_else.cpp` | Conditionals and ternary operators |
| `switch.cpp` | Switch-case statements |
| `for_loops.cpp` | For loops |
| `while_loops.cpp` | While loops |
| `for_each_loops.cpp` | Range-based for loops |
| `arrays.cpp` | Arrays, `sizeof`, and vectors |
| `references.cpp` | References and memory addresses |
| `enums.cpp` | Enumerations |
| `structures.cpp` | Structs (anonymous and named) |

## Running

Compile any file with `g++` and run the output:

```bash
g++ -o output <filename>.cpp && ./output
```